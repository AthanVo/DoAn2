@model IEnumerable<RapChieuPhim.Models.Chi_tiet_dat_ve>

@{
    Layout = null;
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Danh sách chi tiết đặt vé</title>
</head>
<body>
    
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.chi_tiet_dat_ve_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dat_ve_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.gia_ve)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ghe_id)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <th>
                @Html.DisplayFor(modelItem => item.chi_tiet_dat_ve_id)
            </th>
            <td>
                @Html.DisplayFor(modelItem => item.dat_ve_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.gia_ve)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ghe_id)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.chi_tiet_dat_ve_id }) |
                @Html.ActionLink("Xem", "Details", new { id = item.chi_tiet_dat_ve_id }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.chi_tiet_dat_ve_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
